package com.animalguard.sistema.repository;

import com.animalguard.sistema.entity.AnimalPerdido;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AnimalPerdidoRepository extends JpaRepository<AnimalPerdido, Integer> {
}
